apiVersion: v1
kind: Service
metadata:
  name: jobportal-frontend
  namespace: jobportal
spec:
  type: NodePort

  selector:
    app: jobportal-frontend

  ports:
    - name: http
      port: 80
      targetPort: http
      nodePort: 30080
